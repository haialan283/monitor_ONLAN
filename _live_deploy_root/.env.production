# Production defaults for LIVE deploy (Demo System / Docker).
# Override via platform env if needed. Do not put private PINs here.
SECRET_KEY=MonitorTournamentSecretKey2026!
DEPLOY_MODE=LIVE
HOST=0.0.0.0
DISCONNECT_MS=12000
NET_TCP_TIMEOUT_MS=8000
